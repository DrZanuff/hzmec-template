import type { SearchBarProps } from './SearchBar.types'
import * as S from './SearchBar.styles'

export function SearchBar({ value }: SearchBarProps) {
  return <S.SearchBarContainer></S.SearchBarContainer>
}
